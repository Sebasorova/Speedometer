  - name: Compile Minecraft Spigot (via BuildTools)
  # You may pin to the exact commit or the version.
  # uses: SpraxDev/Action-SpigotMC@fd68dfb62156c984d25131f85d56c8a17bfe01c6
  uses: SpraxDev/Action-SpigotMC@v5.0.1
  with:
    # Versions to build (separate multiple with ',')
    versions: 5.1dev
    # Should sources be generated?
    generateSrc: false
    # Should the documentation be generated?
    generateDoc: true
          
